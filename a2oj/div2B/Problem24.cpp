//
// Created by prituladima on 8/17/18.
//

#include <bits/stdc++.h>
#include <algorithm>

#define forr(i, s, n) for (int i = s; i < (int)(n); ++i)
#define forn(i, n) forr(i, 0, n)
#define foreach(cur, map) for(pair<char, int> cur : map)
#define ll long long

using namespace std;

int main() {

    int n, k;

    cin >> n >> k;

    int a[n];

    forn(i, n) cin >> a[i];




    return 0;
}